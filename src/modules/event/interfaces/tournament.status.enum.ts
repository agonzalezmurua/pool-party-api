export enum TournamentStatus {
  /** Tournaments that are being organized */
  active = 'active',
  /** Tournaments that have ended */
  inactive = 'inactive',
  /** Tornaments that have been added on a batch operation */
  legacy = 'legacy',
}
